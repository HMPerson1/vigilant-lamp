import { ClickBlockingDirective } from './click-blocking.directive';

describe('ClickBlockingDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickBlockingDirective();
    expect(directive).toBeTruthy();
  });
});
